extends /partial/layout/default.pug

block content
  if users && users.length
    table
      thead
        th First Name
        th Last Name
        th Email
      tbody
        each user in users
          tr
            td= user.firstName
            td= user.lastName
            td= user.email
  else
    div Please run migrations then refresh the page
